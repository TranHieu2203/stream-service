{
  "name": "app-demo",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild src/index.js --bundle --outfile=bundle.js --platform=browser --minify",
    "dev": "esbuild src/index.js --bundle --outfile=bundle.js --platform=browser --watch"
  },
  "dependencies": {
    "mediasoup-client": "^3.6.0",
    "socket.io-client": "^4.5.1"
  },
  "devDependencies": {
    "esbuild": "^0.19.0"
  }
}
